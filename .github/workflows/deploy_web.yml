name: Deploy to GitHub Pages

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
      - run: flutter pub get
      - run: flutter build web --release --base-href "/Precision_Fitness_Flow/"
      - uses: actions/upload-pages-artifact@v3
        with:
          path: build/web/
      - uses: actions/deploy-pages@v4
        id: deployment

